spring:
  application:
    name: survey-app    
survey:
  app:
    redis:
      host-name: redis-server
      port: 6379

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://2886795307-30005-elsy04.environments.katacoda.com/eureka
